<%= form_with(model: employer, local: true) do |form| %>
  <% if employer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employer.errors.count, "error") %> prohibited this employer from being saved:</h2>

      <ul>
      <% employer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><%= f.text_area :first_name %>
    <%= f.label :last_name %><%= f.text_area :last_name %> <br/>
    <%= f.label :city %><%= f.text_area :city %>
    Bio <%= f.text_area :description %>
    <%= f.label :phone_number %><%= f.text_area :phone_number %>
    <%= f.label :email %><%= f.text_area :email %>
    <%= f.label :company_name %><%= f.text_area :company_name %>
    <%= f.label :image%><%= f.file_field :image%>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
